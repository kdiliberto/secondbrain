{
  "name": "second-brain-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Second Brain — routes Claude API calls from the frontend",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
